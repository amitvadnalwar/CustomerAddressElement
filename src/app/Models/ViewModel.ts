export interface  viewModel{
    FirstName:{
      visible:boolean,
      disabled:boolean,
      required:boolean
    }
    LastName:{
      visible:boolean,
      disabled:boolean,
      required:boolean
    }
    
  }